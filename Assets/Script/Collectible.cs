using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Collectible : Pickable
{
    // Start is called before the first frame update
    void Start()
    {
        item.name = "MysteriousCube";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
